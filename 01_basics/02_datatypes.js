"use strict"; //treat all JS code as newer version

// alert(3+3) we aer using nodejs nor browsers

let age = 18            // number => 2 to power 53  // bigint
let name = "dishant"    // string = ""
let isLoggedIn = true   // boolean = true/false
let state;              // undefined
let empty = null        // null => standalone value
                        // symbol => unique

console.log(typeof undefined);  // undefined
console.log(typeof null);       // object